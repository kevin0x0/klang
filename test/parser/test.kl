let a = 2 + 2 * 2 + 1;

return callvararg(f, 1, 2, 3, 4, [ x | x in ... ]);

let makecfg = usercfg -> usercfg.fallback(defaultconfig);

let mycfg = makecfg({ curl = "~", zerocurl = "^", tabstop = 8 });
(a1, a2) -> 3;
let abs = a -> a >= 0 and a or -a;
mycfg.abs = abs;

let m = method -> print "AAA";
let class = { say = m };
class.say();

let f = n -> n <+ 1 and n or f(n - 1) + f(n - 2) where let a = 1;

let Window = inherit {} : {
  local width;
  local height;
  local pos_x;
  local pos_y;
  constructor = method(width, height) => {
    this.width = width;
    this.height = height;
  };
  moveto = method(posx, posy) => {
    this.pos_x = posx;
    this.pos_y = posy;
  };
};


let a0 = foo(x, x + 1) where {
  let x = bar(4);
  let y = 4;
};

let [a1, b, [c, d]] = getarray(0, 100);

let 4, [(last_inner_left, _)] = expr;

with = (obj, func) => {
  func(obj);
  obj.close();
};

(a, b) -> a + b;

with = obj -> func -> (func(), obj.close());

let fj = ((a, b)) => (p, q);

f ();

let f0 = n -> n + 1;

f.'+' = method rop -> n -> this n + rop;

let g = f + 1;


let [a2, b0], [1, 3, { name = name }] = foo(3);

let class0 = {
  local name;
  local age,
  gender;
};
