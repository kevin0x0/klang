Usage:
  lexgen [ opt [opt-value] | <key>=<value> ]...

Options:
  -h --help                        Show this message.
  -i --in                          Input file path.
  -o <path> --out <path>           Output file path.
  --out-inc <path>                 Output header file path. This option works
                                   only when the language needs a header file,
                                   such as C/C++.
  -t --tmpl <path>                 Specify template file for generating source
                                   file.
  --it --inc-tmpl <path>           Specify template file for generating header
                                   file.
  --table-only                     Only generate the transition table.
  -l=<value> --lang[uage]=<value>  Specify language.
    c                              This is the default value.
    cpp
    rust                           Currently not supported.
