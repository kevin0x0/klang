return callvararg(f, 1, 2, 3, 4, [ x | x in ... ]);

let makecfg = usercfg -> usercfg.fallback(defaultconfig);

let mycfg = makecfg({ curl = "~", zerocurl = "^", tabstop = 8 });
(a1, a2) -> 3;
let abs = a if a >= 0 else -a;
mycfg.abs = abs;

let method = .self -> print "AAA";
let class = { say = method };
class.say();

let f = .n -> n if n <= 1 else f(n - 1) + f(n - 2);
